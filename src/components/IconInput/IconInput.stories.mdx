import { Meta, Story } from '@storybook/addon-docs/blocks';
import IconInput from './IconInput';

<Meta
  title="IconInput"
  component={IconInput}
  argTypes={{
    placeholder: {
      control: {
        type: 'text',
      },
    },
    icon: {
      control: {
        type: 'select',
        options: ['search', 'at-sign'],
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'large'],
      },
    },
    label: {
      control: {
        type: 'text',
      },
    },
  }}
/>

export const Template = (args) => <IconInput {...args} />;

# IconInput

<Story
  name="Default"
  args={{
    placeholder: 'Search…',
    label: 'Search',
    icon: 'search',
    size: 'small',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Small"
  args={{
    placeholder: 'Search…',
    label: 'Search',
    icon: 'search',
    size: 'small',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Large"
  args={{
    placeholder: 'Search…',
    label: 'Search',
    icon: 'search',
    size: 'large',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="WithSpecifiedWitdh"
  args={{
    placeholder: 'Search…',
    label: 'Search',
    icon: 'search',
    size: 'large',
    width: 600
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="WithEmailIcon"
  args={{
    placeholder: 'name@example.com',
    label: 'Email',
    icon: 'at-sign',
    size: 'large',
    width: 600
  }}
>
  {Template.bind({})}
</Story>
